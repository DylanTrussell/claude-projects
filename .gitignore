# Generated build output — rebuilt from game/ + assets/ via tools/assemble.sh
public/

# Downloaded CDN asset mirrors for local Playwright testing — large, regenerable
tools/local_tunnel_cache/
tools/local_ptboat_cache/
tools/local_boss2_cache/
tools/node_modules/

# Scratch/output from past sessions (screenshots, backups, generation drafts)
work/
handoff_extra/
tools/flow_tmp/

# Stale deploy zip artifacts — the real deploy zip is built fresh each time via assemble.sh + zip
*.zip
